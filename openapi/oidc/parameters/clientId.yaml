name: clientId
in: path
description: The UUID or name of the client application (or "me" to use the client_id claim from the access token).
required: true
allowEmptyValue: false
schema:
  type: string