{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "oneOf": [
    {
      "description" : "A UUID reference to a pre-defined rate plan.",
      "$ref" : "RatePlanDefinitionReference.json"
    },
    {
      "description" : "The details of an inline rate plan.",
      "$ref" : "InlineRatePlanDefinitionValues.json"
    }
  ],
  "$id" : "https://schema.autheus.com/ratelimits/RatePlanOrReference.json",
  "title" : "RatePlanOrReference",
  "description" : "This object contains either a UUID reference to a pre-defined rate plan or the details of an inline rate plan which cannot be used in any other API key rules."
}