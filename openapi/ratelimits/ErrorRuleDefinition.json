{
  "type" : "object",
  "properties" : {
    "usePlan" : {
      "description" : "OPTIONAL. When a static API key is specified in the rule, but an API key in the request does not have an associated rate plan, this rate plan is used instead as a 'default'.",
      "$ref" : "RatePlanReference.json"
    }
  },
  "required" : [ "usePlan" ],
  "title" : "ErrorRuleDefinition",
  "description" : "Defines error handling for a static API key rule when the API key isn't found (is not predefined)."
}
